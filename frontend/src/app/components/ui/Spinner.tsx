import React from 'react'; // 导入 React 库

const Spinner: React.FC = () => {
  return (
    <div className="spinner">
      <div className="spinner-inner"></div>
    </div>
  );
};

export default Spinner; // 导出 Spinner 组件